name: "🔄 Update Blog"

on:
  schedule:
    - cron: '0 0 * * *' # 매일 정각 자동 실행 (Daily Run)
  workflow_dispatch:    # 수동 실행 지원 (Manual Trigger)

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 레포지토리에 커밋할 수 있는 권한 부여
    steps:
      - name: Checkout
        uses: actions/checkout@v4      

      # 1. Python 환경 구축 및 품질 관리 (Quality Control)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install feedparser requests ruff

      - name: Lint with Ruff
        # 스크립트의 코드 품질 검사 수행 (정적 분석)
        run: ruff check scripts/update_readme.py 

      # 2. 블로그 포스트 동기화 스크립트 실행 (환경 변수 주입)
      - name: Run update script
        env:
          # RSS 피드 주소를 환경 변수로 관리 (Config as Code)
          RSS_URL: "https://buenhyden.github.io/index.xml" 
        run: python scripts/update_readme.py

      # 3. 모든 변경 사항 통합 커밋 및 푸시 (Unified Commit)
      - name: Commit and Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # 리드미 파일만 추가
          git add README.md 
          if ! git diff --staged --quiet; then
            git commit -m "docs: sync latest blog posts and metrics [skip ci]"
            git push
          fi